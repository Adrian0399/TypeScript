(() => {

    let flash = {
        name: 'Steve Rogers',
        age: 24,
        powers: ['Vida infinita', 'SuperFuerza']
    }

    let superman: { name: string, age?: number, powers: string[], getName?: () => string }  = {
        name: 'Clark Kent',
        age: 60,
        powers: ['Súper velocidad']
    }

})()